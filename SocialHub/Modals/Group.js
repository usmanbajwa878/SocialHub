
class Group {
    constructor(id, name, groupImage, membersCount, isJoined) {
        this.id = id;
        this.name = name;
        this.groupImage = groupImage;
        this.membersCount = membersCount
        this.isJoined = isJoined
    }
}
export default Group;